# Vite dev proxy target (backend API). Default matches Esiana backend on port 3000.
# If your backend runs on 3001, set: VITE_API_PROXY_TARGET=http://localhost:3001
VITE_API_PROXY_TARGET=http://localhost:3000
