# Vite dev proxy target (backend API). Default matches backend/.env.example PORT=3001.
# Override if your backend listens elsewhere, e.g. VITE_API_PROXY_TARGET=http://localhost:3000
VITE_API_PROXY_TARGET=http://localhost:3001
