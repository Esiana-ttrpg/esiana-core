# Verified via CodeQL DB inspection: relative imports (./ssrfGuard.js) do not bind in
# API::moduleImport; scoped workspace packages do. fetchPluginManifest.ts imports
# from '@esiana/ssrf-guard'.
extensions:
  - addsTo:
      pack: codeql/javascript-all
      extensible: barrierGuardModel
    data:
      - ["@esiana/ssrf-guard", "Member[isUrlSafeForImportSync].Argument[0]", "true", "request-forgery"]
